// Fixed set of bookable time slots. Frontend renders these in a dropdown,
// backend validates reservations against the same list.
const TIME_SLOTS = ['12:00', '13:30', '18:00', '19:30', '21:00'];

module.exports = { TIME_SLOTS };
